public class WrappCl {
    public static void main(String[] args) {
        // wrapper class --- use primitive datatypes as reference datatypes
        // they can contain some useful methods
        // can be used as the collections (ex Arraylist)


        // Strings are also there
        // not in the list

        // FASTER                             // SLOWER
        // primitive                         Wrapper
        // ---------                        -------
        // boolean                          //Boolean
        // char                             //Char
        // int                              //Integer
        // double                           //Double


        // features in java
        // autoboxing
        // unboxing

        // autoboxing === automatically java makes the conversion between the suitable datatypes of primitive and their corresponding object wrapper class



        // example
        Boolean a = true;
        Character b = 'm';
        Double d = 12.91;
        Integer g = 1205;
        String f = "manminder";


        //some useful methods
        //same like strings which we have done in previous video



        // unboxing === conversion of wrapper class to primitive just the opposite
        if (a ==true) {
            System.out.println("this is true");

            // same with others
        }


        // unboxing ===


    }
}
