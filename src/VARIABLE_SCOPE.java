public class VARIABLE_SCOPE {
    public static void main(String[]args){
        // local
        // global

        // local = declared inside the method
        //global = declared OUTSIDE the method BUT WITHIN CLASS

        // visibility
        // local = only visible in the declared method
        // global = Visible to all parts of the class means all methods that declared in that class


Dice_Roller_VARSCOPE DR = new Dice_Roller_VARSCOPE();
    }
}
